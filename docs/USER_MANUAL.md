# dietolog_client — руководство пользователя

Краткое описание приложений «Диетолог».  
При изменении UI обновлять этот файл и выкладывать в репозиторий вместе с кодом (см. `.cursor/rules/project-context.mdc`).

**Репозиторий:** https://github.com/RobinZGit/dietolog_client  

---

## Два интерфейса

| Интерфейс | Зачем |
|-----------|--------|
| **Angular** (`ng serve`) | Полная раскладка рациона, нормы, спектры, сохранение `.dtlg` |
| **Простой HTML** [`simple/dietolog.html`](../simple/dietolog.html) | Быстрый просмотр: продукты → нутриенты и количество на 100 г (IndexedDB) |

Ссылка **«простой просмотр»** в заголовке Angular открывает `simple/dietolog.html` в новой вкладке.

- **GitHub Pages:** https://robinzgit.github.io/dietolog_client/simple/dietolog.html  
- Локально / `ng serve`: `/simple/dietolog.html`

---

## Простой просмотр (`simple/dietolog.html`)

1. Откройте файл (или ссылку из Angular).
2. При первом открытии снимок БД записывается в **IndexedDB** браузера (`dietolog_simple`).
3. В поле поиска введите часть имени продукта (можно несколько через `|`).
4. Нажмите на продукт — раскроется таблица нутриентов и значений **на 100 г**.
5. Повторное открытие читает данные из IndexedDB (быстро); при смене версии снимка база перезаливается.

Это **минимальный** UI: без редактирования рациона и без расчёта дневной нормы. Дальнейшие действия формы — по отдельным указаниям.

---

## Angular: продукты и нутриенты

### Запуск

```bash
npm install
npm start
```

Обычно `http://localhost:4200/`. Опционально сервер `dietolog_server` на `:8080` при `localData = false`.

### Продукты

| Элемент | Смысл |
|---------|--------|
| Поле количества (гр.) | Сколько продукта в раскладке |
| Кнопка `>` у продукта | Добавить «оптимальное» количество продукта |
| Чекбокс «Исключен» | Продукт не участвует в расчёте |
| «спектр» | Вклад нутриентов выбранного продукта |
| Фильтр по подстроке | Поднять в списке (`\|` = ИЛИ) |
| Сортировка по нутриенту | По содержанию выбранного нутриента |
| ADD | Добавить рекомендованный продукт |
| Save / `.dtlg` | Выгрузка / загрузка раскладки |

### Нутриенты

Текущее значение по раскладке, min/max нормы, исключение, разворот дерева продуктов, спектр, ссылки edaplus.info.

### Состояние в браузере

`localStorage` ключи `rz_dietolog_configuration_*`.

---

## Данные

- Снимок: ~43 нутриента, ~1231 продукт, ~26k значений на 100 г.
- По йоду и ряду микроэлементов значения есть не у всех продуктов — ограничение исходной таблицы.
- Более широкие БД (USDA SR Legacy ~7.8k продуктов, до ~150 компонентов) проанализированы; полный импорт в простой клиент пока не делался (другой каталог имён, объём, время на маппинг).

---

## Связанные проекты

| Проект | Роль |
|--------|------|
| [dietolog_server](https://github.com/RobinZGit/dietolog_server) | API + PostgreSQL `dietolog` |
| [UN_Calculator](https://github.com/RobinZGit/UN_Calculator) | Образец простого HTML+форма |
| [MultiLogicTradePg](https://github.com/RobinZGit/MultiLogicTradePg) | Образец: контекст и инструкции в репозитории |
